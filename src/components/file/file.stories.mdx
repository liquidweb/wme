import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { File } from "..";
import fileUploadImage from '../../stories/images/file-upload.png';

<Meta
  title="Forms/File"
  component={File}
  argTypes={{
    filePath: {
      control: "string",
      description: "Path to the selected file",
    },
    imagePath: {
      control: "string",
      description: "Path to the selected image",
    },
    imageAlt: {
      control: "string",
      description:
        "Provides alternative information for an image if a user for some reason cannot view it.",
    },
  }}
/>

export const Template = (args) => <File {...args} />;

# File

## Props

<ArgsTable story="." />

## Example

```
import React, { useState } from 'react';
import { File } from '@moderntribe/wme';

export const MyFile = (props) => (
  <File {...props} />
);

export default MyFile;
```

## Canvas

<Canvas>
  <Story
    name="File"
    args={{
      filePath: "~/Downloads/document.pdf",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Image"
    args={{
      imageAlt: "handwritten signature",
      imagePath: fileUploadImage,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
