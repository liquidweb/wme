import { useState } from 'react';
import {
  ArgsTable,
  Story,
  Canvas,
  Meta,
} from '@storybook/addon-docs';
import { Typography, Link, Grid } from '@mui/material';
import { CardSelectGroup, CardSelectItem, Chip, FormHelperText } from '..';

import cardSelectImage from '../../stories/images/card-select-item.png';

<Meta
  title="Misc/CardSelectGroup"
  component={CardSelectGroup}
  argTypes={{
    orientation: {
      control: {
        type: 'inline-radio',
        options: ['horizontal', 'vertical'],
      },
      defaultValue: 'horizontal',
      description: 'Change direction of CardSelectItem(s)',
      table: {
        type: {
          summary: 'string',
          detail: "'horizontal' | 'vertical'"
        },
        defaultValue: {
          summary: 'horizontal',
        }
      }
    },
    exclusive: {
      control: 'boolean',
      defaultValue: false,
      description: 'If `true`, only allow one of the child CardSelectItem values to be selected.',
      table: {
        type: {
          summary: 'boolean',
        },
        defaultValue: {
          summary: 'false',
        }
      }
    },
    cardColumns: {
      control: { 
        type: 'range',
        min: 1,
        max: 6,
        step: 1,
      },
      defaultValue: 3,
      description: 'Cards per row',
      table: {
        type: {
          summary: 'number',
        },
      }
    },
    cardPadding: {
      control: {
        type: 'inline-radio',
        options: ['sm', 'md'],
      },
      defaultValue: 'sm',
      description: 'Change padding value on CardSelectItems',
      table: {
        type: {
          summary: 'string',
          detail: "'sm' | 'md'"
        },
        defaultValue: {
          summary: 'sm',
        }
      }
    },
    cardSpacing: {
      control: { 
        type: 'range',
        min: 0,
        max: 4,
        step: 1,
      },
      defaultValue: 1,
      description: 'Change padding value on CardSelectItems',
      table: {
        type: {
          summary: 'number',
        },
      }
    },
    value: {
      control: false,
      defaultValue: '1',
      description: 'The currently selected value within the group or an array of selected values when `exclusive` is false. The value must have reference equality with the option in order to be selected.',
      table: {
        type: {
          summary: 'any',
        },
      }
    },
    children: {
      control: false,
      description: 'The content of the component.',
      table: {
        type: {
          summary: 'node',
        },
      }
    },
  }}
/>

export const Group = (args) => {
  const [selectedIndex, setSelectedIndex] = useState(() => ['1']);
  const handleIndexChange = (event, value) => {
    setSelectedIndex(value);
  }
  return (
    <CardSelectGroup
      {...args}
      value={selectedIndex}
      onChange={handleIndexChange}
    >
      <CardSelectItem
        primary="Vestibulum id ligula porta felis euismod semper"
        secondary="Cras justo odio, dapibus ac facilisis in, egestas eget quam. Nullam id dolor id nibh ultricies vehicula ut id elit."
        footer="Presented by Modern Tribe"
        icon={cardSelectImage}
        value="1"
      />
      <CardSelectItem
        primary="Example of a disabled card"
        secondary="Donec ullamcorper nulla non metus auctor fringilla."
        footer="Presented by Modern Tribe"
        icon={cardSelectImage}
        value="2"
        disabled
      />
      <CardSelectItem
        primary="Example of card with alternative completed icon"
        secondary="Praesent commodo cursus magna, vel scelerisque nisl consectetur et."
        completedIcon={<Chip size="small" color="success" label="Complete" />}
        footer="Presented by Modern Tribe"
        icon={cardSelectImage}
        value="3"
      />
      <CardSelectItem
        primary="Hello"
        secondary="Fusce dapibus, tellus ac cursus commodo, tortor mauris condimentum nibh, ut fermentum massa justo sit amet risus."
        footer="Presented by Modern Tribe"
        icon={cardSelectImage}
        value="4"
      />
      <CardSelectItem
        primary="Praesent commodo cursus magna"
        secondary="Maecenas sed diam eget risus varius blandit sit amet non magna."
        footer="Presented by Modern Tribe"
        icon={cardSelectImage}
        value="5"
      />
      <CardSelectItem
        primary="Hello World"
        secondary="Donec ullamcorper nulla non metus auctor fringilla."
        footer="Presented by Modern Tribe"
        icon={cardSelectImage}
        value="6"
      />
    </CardSelectGroup>
  );
};

# Card Select Group
Card Selects are used when information is best presented paired with 
simple imagery like an icon. This allows the user to select one option,
or in some cases select multiple.

## Props
<ArgsTable story="." />

## Horizontal
<Canvas>
  <Story name="Horizontal" args={{ orientation: 'horizontal' }}>
    {Group.bind({})}
  </Story>
</Canvas>

## Vertical
<Canvas>
  <Story name="Vertical" args={{ orientation: 'vertical' }}>
    {Group.bind({})}
  </Story>
</Canvas>

- [Card Select Item](/docs/misc-cardselectitem--alt-completed-icon)
