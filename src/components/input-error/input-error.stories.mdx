import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { FormFieldControl, InputError } from "..";

<Meta
  title="Forms/InputError"
  component={InputError}
  argTypes={{
    children: {
      control: "text",
      defaultValue: "This field is required.",
      description: "The content (children) to appear in the error div",
    },
    error: {
      control: "boolean",
      defaultValue: false,
      description: "If true, the input will indicate an error. The prop defaults to the value (false), and is inherited from the `useFormControlUnstyledContext` hook."
    },
  }}
/>

export const Template = ({ error, ...args }) => (
  <FormFieldControl error={error}>
    <InputError {...args} />
  </FormFieldControl>
);

# Input Label

A styled version of <a href="https://mui.com/material-ui/react-typography/" target="_blank">MUI's Typography</a>, so you can use any prop provided there.

## Usage

- Always wrap with a `FormFieldControl` component in order to use `useFormControlUnstyledContext` hook to determine if input is invalid.

## Example

```
import React, { useState } from 'react';
import { FormFieldControl, InputError } from '@moderntribe/wme';

export const MyInputError = (props) => (
  <FormFieldControl>
    <InputError>
      This field is required.
    </InputError>
  </FormFieldControl>
);

export default InputError;
```

## Canvas

<Canvas>
  <Story name="Base">{Template.bind({})}</Story>
</Canvas>
