import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import InputError from "./";

<Meta
  title="Forms/InputError"
  component={InputError}
  argTypes={{
    children: {
      control: "text",
      defaultValue: "This field is required.",
      description: "The content (children) to appear in the error div",
    },
  }}
/>

export const Template = (args) => <InputError {...args} />;

# Input Label

A styled version of <a href="https://mui.com/material-ui/react-typography/" target="_blank">MUI's Typography</a>, so you can use any prop provided there.

## Example

```
import React, { useState } from 'react';
import { InputError } from '@moderntribe/wme';

export const MyInputError = (props) => (
  <InputError>This field is required.<InputError>
);

export default InputError;
```

## Canvas

<Canvas>
  <Story name="Base">{Template.bind({})}</Story>
</Canvas>
