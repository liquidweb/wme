import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { FileUpload } from "..";

<Meta title="Forms/FileUpload" component={FileUpload} />

export const Template = (args) => <FileUpload {...args} />;

# FileUpload

A styled version of <a href="https://mui.com/material-ui/api/input/" target="_blank">MUI's Input</a>, so you can use any prop provided there.

## Props

<ArgsTable story="." />

## Example

```
import React, { useState } from 'react';
import { FileUpload } from '@moderntribe/wme';

export const MyFileUpload = (props) => (
  <FileUpload {...props} />
);

export default MyFileUpload;
```

## Canvas

<Canvas>
  <Story name="Base">{Template.bind({})}</Story>
</Canvas>
