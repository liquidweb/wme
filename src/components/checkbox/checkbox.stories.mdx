import {
  ArgsTable,
  Story,
  Canvas,
  Meta,
} from '@storybook/addon-docs';
import { Checkbox } from '..';

<Meta
  title="Input/Checkbox"
  component={Checkbox}
  argTypes={{
    inputLabel: {
      control: 'text',
      description: 'Descriptive label of checkbox group (optional)',
    },
    label: {
      control: 'text',
      description: 'Text that corresponds with checkbox',
    },
    error: {
      control: 'boolean',
      description: 'True/false value for displaying an error',
    },
    errorMessage: {
      control: 'text',
      description: 'Error message, only shows if "error" === true',
    },
  }}
/>

export const Template = (args) => <Checkbox {...args} />;

# Checkbox
Checkboxes allow users to select one or more items from a set of options.

## Usage
- Checkboxes should be used independently from each other.
- Ensure both label and input are clickable to select the checkbox field.

## Props
<ArgsTable story="." />

## Example
```
import React, { useState } from 'react';
import { SplitButton } from '@moderntribe/wme';

export const MyCheckbox = (props) => (
  <>
    <h1>Checkbox</h1>
    <Checkbox
      inputLabel="My checkbox"
      label="Check this box, or uncheck it"
      error={props.error}
      errorMessage={props.errorMessage}
    />
  </>
);

export default MyCheckbox;
```
## Canvas
<Canvas>
  <Story
    name="Base"
    args={{
      label: 'Creating an account means you’re okay with our Terms of Service and Privacy Policy.',
      error: false,
      errorMessage: 'Error Message',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
