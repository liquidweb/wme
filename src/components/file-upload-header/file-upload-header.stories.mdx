import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { FileUploadHeader } from "..";

<Meta
  title="Forms/FileUploadHeader"
  component={FileUploadHeader}
  argTypes={{
    buttonText: {
      control: "text",
      description: "Text to display inside remove file button.",
    },
    fileUploaded: {
      control: "boolean",
      description:
        "Defines whether or not there is an uploaded filed asscociated with this input.",
    },
    labelText: {
      control: "text",
      description: "Input label text",
    },
    onRemove: {
      description: "Callback fired when the remove file button is clicked.",
      table: {
        type: {
          summary: "(e: React.MouseEvent<HTMLButtonElement>) => void",
        },
      },
    },
  }}
/>

export const Template = (args) => <FileUploadHeader {...args} />;

# FileUploadHeader

Composes `Button`, and `InputTitle` to create a single, re-usable "FileUploadHeader" component.

**_Note:_** Any additional props passed to this component will be passed to <a href="https://mui.com/material-ui/api/box/" target="_blank">MUI's Box</a>, so you can use any prop provided there.

## Props

<ArgsTable story="." />

## Example

```
import React, { useState } from 'react';
import { FileUploadHeader } from '@moderntribe/wme';

export const MyFileUploadHeader = (props) => (
  <FileUploadHeader {...props} />
);

export default MyFileUploadHeader;
```

## Canvas

<Canvas>
  <Story
    name="Base"
    args={{
      labelText: "Upload File",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Uploaded"
    args={{
      buttonText: "Delete",
      labelText: "Upload File",
      fileUploaded: true,
      sx: {
        maxWidth: 300
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
