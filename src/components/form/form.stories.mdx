import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { Grid, Stack } from "@mui/material";
import { Form, TextField } from "..";

<Meta
  title="Forms/Form"
  component={Form}
  argTypes={{
    children: {
      description:
        "Any valid form elements such as `FormField`, `FormControl`, `InputLabel`, etc.",
    },
  }}
/>

export const Template = (args) => <Form {...args} />;

# Form

A styled version of <a href="https://mui.com/material-ui/react-box" target="_blank">MUI's Box</a>, so you can use any prop provided there.

## Props

<ArgsTable story="." />

## Example

```
import React from 'react';
import { Form } from '@moderntribe/wme';

export const MyForm = (props) => (
  <Form>
    <TextField
      label="Text Input"
    />
  </Form>
)

export default Form;
```

## Canvas

<Canvas>
  <Story
    name="Base"
    args={{
      children: <TextField label="Text Field" />,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Column"
    args={{
      children: (
        <Stack spacing={2}>
          <TextField label="First Name" />
          <TextField label="Last Name" />
          <TextField label="Email Address" />
          <TextField label="Phone Number" />
        </Stack>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Grid"
    args={{
      children: (
        <Grid container spacing={2}>
          <Grid item xs={6}>
            <TextField label="First Name" />
          </Grid>
          <Grid item xs={6}>
            <TextField label="Last Name" />
          </Grid>
          <Grid item xs={6}>
            <TextField label="Email Address" />
          </Grid>
          <Grid item xs={6}>
            <TextField label="Phone Number" />
          </Grid>
        </Grid>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
