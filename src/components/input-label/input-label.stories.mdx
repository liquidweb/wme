import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { InputLabel, CheckboxInput } from "..";

<Meta
  title="Forms/InputLabel"
  component={InputLabel}
  argTypes={{
    control: {
      control: "select",
      defaultValue: "checkbox",
      description: "A control element.",
      mapping: {
        checkbox: <CheckboxInput />,
      },
      options: ["checkbox"],
      type: { required: true },
    },
    label: {
      control: "text",
      defaultValue:
        "Creating an account means you’re okay with our Terms of Service and Privacy Policy.",
      description: "Text or element to be used in an enclosing label element.",
    },
    labelPlacement: {
      control: {
        type: "select",
        labels: {
          bottom: "Bottom",
          end: "End",
          start: "Start",
          top: "Top",
        },
      },
      description: "The position of the label.",
      options: ["bottom", "end", "start", "top"],
    },
  }}
/>

export const Template = (args) => <InputLabel {...args} />;

# InputLabel

A styled version of <a href="https://mui.com/material-ui/api/form-control-label" target="_blank">MUI's FormControlLabel</a>, so you can use any prop provided there.

## Props

<ArgsTable story="." />

## Example

```
import React, { useState } from 'react';
import { InputLabel, CheckboxInput } from '@moderntribe/wme';

export const MyInputLabel = (props) => (
  <InputLabel
    control={<CheckboxInput />}
    label="Creating an account means you’re okay with our Terms of Service and Privacy Policy."
  />
);

export default MyInputLabel;
```

## Canvas

<Canvas>
  <Story name="Base">{Template.bind({})}</Story>
</Canvas>
