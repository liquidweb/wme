import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { CheckboxInput } from "..";

<Meta title="Forms/CheckboxInput" component={CheckboxInput} />

export const Template = (args) => <CheckboxInput {...args} />;

# CheckboxInput

A styled version of <a href="https://mui.com/material-ui/react-checkbox/" target="_blank">MUI's Checkbox</a>, so you can use any prop provided there.

## Props

<ArgsTable story="." />

## Example

```
import React, { useState } from 'react';
import { CheckboxInput } from '@moderntribe/wme';

export const MyCheckboxInput = (props) => (
  <CheckboxInput {...props} />
);

export default MyCheckboxInput;
```

## Canvas

<Canvas>
  <Story name="Base">{Template.bind({})}</Story>
</Canvas>
