import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import FormControl from "./";
import InputError from "../input-error";
import InputLabel from "../input-label";
import TextInput from "../text-input";

<Meta
  title="Forms/FormControl"
  component={FormControl}
  argTypes={{
    error: {
      control: "boolean",
      description: "If true, the input will indicate an error.",
    },
  }}
/>

export const Template = (args) => <FormControl {...args} />;

# Input Label

A styled version of <a href="https://mui.com/base/react-form-control/" target="_blank">MUI's FormControlUnstyled</a>, so you can use any prop provided there.

## Example

```
import React, { useState } from 'react';
import { FormControl } from '@moderntribe/wme';

export const MyFormControl = (props) => (
  <FormControl>
    ...
  <FormControl>
);

export default MyFormControl;
```

## Canvas

<Canvas>
  <Story
    name="Base"
    args={{
      children: (
        <>
          <InputLabel>Field Label</InputLabel>
          <TextInput />
          <InputError>This field is invalid.</InputError>
        </>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
