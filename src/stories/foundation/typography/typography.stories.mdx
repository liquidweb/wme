import { Meta } from "@storybook/addon-docs";
import { Box, Typography } from "@mui/material";
import TypographyDetails from "./typography-details";
import { capitalize } from "../../../utils";
import { WMEProvider } from "../../../theme";

<Meta title="Foundation/Typography" />

## Overview

Typography works by principle of accessibility before aesthetics.
Text should always be readable and help the user understand what is important by well constrasted size and color.
We use the users default System-Font.

- Official MUI typography documantion: https://mui.com/system/typography/

## Usage and examples

<WMEProvider>
  <Box sx={{ mt: 2 }}>
    {["h1", "h2", "h3", "h4", "h5", "body", "subtext", "link"].map(
      (variant) => (
        <Box key={variant}>
          <Typography variant={variant}>
            {`${capitalize(variant)} — Mithrandir, The Gray Wizard`}
          </Typography>
          <TypographyDetails variant={variant} />
        </Box>
      )
    )}
  </Box>
</WMEProvider>
