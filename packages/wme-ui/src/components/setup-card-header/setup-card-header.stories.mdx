import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { SetupCardHeader, Chip } from "..";
import { todo } from "../chip/chip.stories.mdx";

<Meta
	title="Setup Cards/SetupCardHeader"
	component={SetupCardHeader}
	argTypes={{
		title: {
			type: "string",
			description: "The content (title) of the component.",
			table: {
				type: {
					summary: "node",
				},
			},
		},
		subheader: {
			type: "string",
			description: "The content(subtitle) of the component.",
			table: {
				type: {
					summary: "node",
				},
			},
		},
		isComplete: {
			type: "boolean",
			description: "Shows a green checkmark beside the title.",
			table: {
				type: {
					summary: "node",
				},
			},
		},
		chipText: {
			type: "string",
			description: "The content of the chip.",
			table: {
				type: {
					summary: "node",
				},
			},
		},
		chipBackground: {
			control: "select",
			options: [
				"info",
				"primary",
				"success",
				"secondary",
				"error",
				"warning",
			],
			description: "The background color of the chip",
			table: {
				type: {
					summary: "node",
				},
			},
		},
	}}
	decorators={[
		(Story) => (
			<div style={{ maxWidth: 800, margin: "0 auto" }}>
				<Story />
			</div>
		),
	]}
/>

export const Template = (args) => <SetupCardHeader {...args} />;

# Setup Card Header

The Wizard header features the logo and the ability to exit.

## Props

<ArgsTable story="." />

<Canvas>
	<Story
		name="Default"
		args={{
			title: "Example Card Header",
			subheader: "Donec ullamcorper nulla non metus auctor fringilla.",
			isComplete: false,
		}}
	>
		{Template.bind({})}
	</Story>
	<Story
		name="With Complete"
		args={{
			title: "Example Card Header",
			subheader: "Donec ullamcorper nulla non metus auctor fringilla.",
			isComplete: true,
		}}
	>
		{Template.bind({})}
	</Story>
	<Story
		name="With a Chip"
		args={{
			title: "Example Card Header",
			subheader: "Donec ullamcorper nulla non metus auctor fringilla.",
			isComplete: false,
			chipText: "Lorum",
			chipBackground: "info",
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>
