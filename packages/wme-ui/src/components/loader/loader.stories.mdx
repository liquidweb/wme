import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { Loader } from "..";

<Meta
	title="Misc/Loader"
	component={Loader}
	argTypes={{
	}}
/>

export const Template = (args) => <Loader {...args} />;

# Loader
Loader is a component that displays a loading animation.

## Example

```
import React from 'react';
import { Loader } from '@moderntribe/loader';

const myLoader = () => {
  <>
    <h1>Loader Component</h1>
    <Loader />
  </>
};

export default myLoader;
```

## Canvas

<Canvas>
	<Story
		name="Default"
	>
		{Template.bind({})}
	</Story>
</Canvas>
