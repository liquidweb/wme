import {
  ArgsTable,
  Story,
  Canvas,
  Meta
} from '@storybook/addon-docs';
import { useState } from 'react';
import { ExitButton } from '..';

<Meta
  title="Misc/Exit"
  component={ ExitButton }
  argTypes={{
    exitText: {
      control: {
        type: 'text',
      },
    }
  }}
/>

export const Template = (args) => {
  return (
    <ExitButton>
      {args.exitText}
    </ExitButton>
  );
};

# Exit
Exit button to close wizard. Takes a string for the text and outputs it with the closing icon.

Extends Box so you can pass SX prop to container if needed.

## Props
<ArgsTable story="." />

## Canvas
<Canvas>
  <Story
    name="Default"
    args={{
      exitText: 'Exit to Setup',
    }}
  >
  {Template.bind({})}
  </Story>
</Canvas>
